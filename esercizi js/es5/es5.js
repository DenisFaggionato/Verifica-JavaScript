//Per generare un valore numeri casuale si può utilizzare la funzione math.random
console.log(Math.random());
//Se vogliamo ad'esempio generare un numero da 0 a 10 basta utilizzare la funzione precedente con la funzione math.floor per arrotondarla in un numero intero e moltiplicare la funzione math.random per il numero del valore massimo che vogliamo aggiungendo 1
console.log(Math.floor(Math.random()*11))
